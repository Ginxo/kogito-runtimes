pipeline{
    agent {label 'operator-slave'}
    stages{
        stage('Update version in pom'){
            steps{
               dir ("${WORKSPACE}") {
                   updateMavenVersion()
              }     
            }
        }
    }
}

def updateMavenVersion(){
    def workspace = manager.build.getEnvVars()["WORKSPACE"]
    String fileContents = new File('${workspace}/pom.xml').text
    println(fileContents)   
}
