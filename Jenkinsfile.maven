pipeline{
    agent {label 'operator-slave'}
    stages{
        stage('Update version in pom'){
            steps{
                sh 'ls -la'
                updateMavenVersion()
            }
        }
    }
}

def updateMavenVersion(){
    def pomXml = new XmlSlurper().parse("pom.xml")
    def mavenVersion = pomXml.project.properties.pom.project.properties
    assert authorResult.text() == '3.6.2'
}
