pipeline{
    agent {label 'kogito-cloud'}
    stages{
        stage('Update version in pom'){
            steps{
               dir ("${WORKSPACE}") {
                   updateMavenVersion()
              }     
            }
        }
    }
}

def updateMavenVersion(){

    def proc = "ls -la /home/jenkins/".execute()
    def b = new StringBuffer()
    proc.consumeProcessErrorStream(b)

    println proc.text
    println b.toString()

}
