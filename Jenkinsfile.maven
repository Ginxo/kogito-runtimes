pipeline{
    agent {label 'operator-slave'}
    stages{
        stage('Update version in pom'){
            steps{
               dir ("${WORKSPACE}") {
                   updateMavenVersion()
              }     
            }
        }
    }
}

def updateMavenVersion(){

    def proc = "ls -la /home/".execute()
    def b = new StringBuffer()
    proc.consumeProcessErrorStream(b)

    println proc.text
    println b.toString()

}
